import React from 'react'

import { AgencyCompanyInfo } from '@brainstorm'

export default {
  title: 'AgencyCompanyInfo',
  argTypes: {
  },
}

function Template () {
  return (
    <AgencyCompanyInfo />
  )
}

export const Primary = Template.bind({})