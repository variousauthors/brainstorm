import React from 'react'

import { ProfileForm } from '@brainstorm'

export default {
  title: 'ProfileForm',
  argTypes: {
  },
}

function Template () {
  return (
    <ProfileForm />
  )
}

export const Primary = Template.bind({})