import React from 'react';

import { Countries } from '@brainstorm';

export default {
  title: 'Countries',
  argTypes: {
  },
};

function Template () {
  return (
    <Countries />
  )
}

export const Primary = Template.bind({});